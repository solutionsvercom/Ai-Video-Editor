## Frontend environment variables (Vite)
#
# Backend API base URL.
# - Recommended for local dev: leave as /api and use Vite proxy (see vite.config.js)
# - Alternative: set to full URL like http://localhost:5000/api
VITE_API_BASE_URL=/api

